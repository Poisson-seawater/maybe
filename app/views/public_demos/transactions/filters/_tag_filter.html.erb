<%# locals: (form:) %>
<div data-controller="list-filter">
  <div class="relative">
    <input type="search" autocomplete="off" placeholder="<%= t('.placeholder') %>" data-list-filter-target="input" data-action="input->list-filter#filter" class="block w-full rounded-md border border-secondary bg-container py-2 pl-10 pr-3 focus:ring-gray-500 sm:text-sm">
    <%= icon("search", class: "absolute inset-y-0 left-2 top-1/2 -translate-y-1/2 transform") %>
  </div>

  <div class="my-2" data-list-filter-target="list">
    <% @demo_tags.each do |tag| %>
      <div class="filterable-item flex items-center gap-2 p-2" data-filter-name="<%= tag.name %>">
        <%= form.check_box :tags,
                           {
                             multiple: true,
                             checked: @q[:tags]&.include?(tag.name),
                             class: "checkbox checkbox--light"
                           },
                           tag.name,
                           nil %>
        <%= form.label :tags, value: tag.name, class: "flex cursor-pointer items-center gap-2 text-sm text-primary" do %>
          <%= render DS::FilledIcon.new(
            variant: :text,
            hex_color: tag.color || Tag::UNCATEGORIZED_COLOR,
            text: tag.name,
            size: "sm",
            rounded: true
          ) %>
          <%= tag.name %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
